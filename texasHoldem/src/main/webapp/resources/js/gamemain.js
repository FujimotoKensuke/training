function roundWinDialogHide() {
    PF('roundWinDialog').hide();
    PF('stepRoundTimer').start();
}

function roundLoseDialogHide() {
    PF('roundLoseDialog').hide();
    PF('stepRoundTimer').start();
}

function roundDrawDialogHide() {
    PF('roundDrawDialog').hide();
    PF('stepRoundTimer').start();
}

